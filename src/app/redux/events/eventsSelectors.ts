export const eventStart = (state: any) => state.newEventStart;
export const allEvents = (state: any) => state.events;
export const currentEvent = (state: any) => state.currentEvent;
export const isLoading = (state: any) => state.isLoading;
